# Konfiguration: Aktienbuchhaltung Betrieb
# Alle KONFIGURIEREN-Felder vor dem ersten Lauf anpassen.

# --- Pfade ---
OBSIDIAN_VAULT="${HOME}/Documents/Obsidian"           # KONFIGURIEREN
BETRIEB_ROOT="${OBSIDIAN_VAULT}/90_Betrieb/Aktienbuchhaltung"
HUGO_PROJECT="${HOME}/sites/depot"                    # KONFIGURIEREN
HUGO_PUBLIC="${HUGO_PROJECT}/public"
DEPOT_QUELLDATEI="${OBSIDIAN_VAULT}/Depot/depot.csv"  # KONFIGURIEREN

# --- Zeitzone ---
TIMEZONE="Europe/Zurich"

# --- Plausibilitätsparameter ---
MAX_ALTER_TAGE=7
TOLERANZ_BETRAG="0.01"
MIN_POSITIONEN=1

# --- CSV-Spalten (0-basiert, anpassen an Depotformat) ---
CSV_SPALTE_ISIN=0
CSV_SPALTE_DATUM=1
CSV_SPALTE_WERT=2
CSV_TRENNZEICHEN=","
